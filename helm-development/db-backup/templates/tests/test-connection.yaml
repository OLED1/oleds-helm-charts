apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "db-backup.fullname" . }}-test-connection"
  labels:
    {{- include "db-backup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "db-backup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
